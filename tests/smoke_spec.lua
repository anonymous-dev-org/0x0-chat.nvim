describe("test harness", function()
  it("loads all surviving zxz modules from runtimepath", function()
    assert.is_truthy(require("zxz"))
    assert.is_truthy(require("zxz.worktree"))
    assert.is_truthy(require("zxz.chat"))
    assert.is_truthy(require("zxz.review"))
    assert.is_truthy(require("zxz.commands"))
    assert.is_truthy(require("zxz.sound"))
  end)
end)
